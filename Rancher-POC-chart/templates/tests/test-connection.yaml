apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Rancher-POC-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Rancher-POC-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Rancher-POC-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
