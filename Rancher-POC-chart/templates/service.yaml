apiVersion: v1
kind: Service
metadata:
  name: {{ include "Rancher-POC-chart.fullname" . }}
  labels:
    {{- include "Rancher-POC-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Rancher-POC-chart.selectorLabels" . | nindent 4 }}
